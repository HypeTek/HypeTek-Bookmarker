# Security

HypeTek Bookmarker stores saved addresses locally in `servers.json` and settings in `settings.json`.

Do not commit personal configuration files, credentials, API keys, passwords or private certificates to the repository.

The application is intended to open saved addresses in the Windows default browser. It does not require administrator privileges and does not store authentication credentials by design.
